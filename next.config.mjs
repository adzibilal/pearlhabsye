/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'res.cloudinary.com',
            'adzibilal.vercel.app',
            'tailwindui.com'
        ]
    }
}

export default nextConfig
